# Пути workspace (договорённости)

| Что | Путь |
|-----|------|
| Каталог проекта (репозиторий) | `/home/oleg/Drawio_poject/` |
| Логи SEAF / draw.io («рабочий каталог» для разбора логов) | `/home/oleg/.config/draw.io/plugins/seaf_plugin/logs` (`seaf-plugin.log`) |

На других машинах замените `/home/oleg` на `$HOME` того пользователя, под которым запускается draw.io.

Дублируется в корневом `README.md` и в `.cursor/rules/workspace-paths.mdc` (для Cursor Agent, `alwaysApply`).
